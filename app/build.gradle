apply plugin: 'com.android.application'
android {
    compileSdkVersion 16

    defaultConfig {
        applicationId "httpproxy.android"
        minSdkVersion 9
        targetSdkVersion 9
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.txt'
        }
    }
}

dependencies {
}